import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#515A6B", // Replace with your desired background color
    },
    primary: {
      main: "#B2B4CB",
    },
    secondary: {
      main: "#515A6B",
    },
  },
});

export default theme;
